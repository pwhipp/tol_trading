broker:
  mode: paper
  currency: USD

actions:
  - target:
      symbol: TSLA.NASDAQ
      percent: 10%
      using: [CASH[USD], NVDA.NASDAQ]
  - target:
      symbol: TSM.NYSE
      percent: 10%
      using: [CASH[USD], NVDA.NASDAQ]
