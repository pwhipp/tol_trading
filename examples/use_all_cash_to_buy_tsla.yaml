version: 1

broker:
  mode: paper

settings:
  slippage: 0.005        # 0.5%
  rounding: FLOOR        # explicit policy

actions:
  - buy:
      symbol: TSLA.NASDAQ
      using: [CASH (USD)]
      quantity: ALL

  - fx:
      from: USD
      to: AUD
      quantity: $1,000 (USD)
