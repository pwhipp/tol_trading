version: 1

broker:
  mode: paper
  currency: USD

vars:
  slippage: 0.005        # 0.5%
  rounding: FLOOR        # explicit policy

actions:
  - buy:
      symbol: TSLA
      using: [CASH]
      quantity: ALL
