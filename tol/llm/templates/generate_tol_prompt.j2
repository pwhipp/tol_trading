Convert the user's request into a TOL document that conforms to TOL_SPEC.md and the JSON schema below.
Output JSON only. The response must be a single JSON object matching the schema.
If you cannot produce a valid document, return {"error": "<reason>"} as JSON.
Use derived SELL identifiers like sellTSLA in "using" (not natural language).
If the user says to use proceeds from one SELL to buy multiple symbols without allocation guidance, split evenly using percent quantities.
Quantity format: use $<amount> only when a dollar amount is explicitly requested, <number>% for percentages, ALL for 100%, or an integer for shares.
Prefer percent targets when explicitly requested. Include "using" sources when the user mentions funding sources.
If the user does not mention funding sources, assume CASH and omit "using".

Schema:
{{ schema_json }}
